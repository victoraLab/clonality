% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_well_contigs.R
\name{analyze_well_contigs}
\alias{analyze_well_contigs}
\title{Analyze Contigs in a Well for Quality and Background Signals}
\usage{
analyze_well_contigs(
  df,
  cell_id_col = "cell_id",
  barcode_format = "WELL_PLATE"
)
}
\arguments{
\item{df}{A data frame output from \code{clonality()} containing at least \code{cell_id}, \code{clonality}, and \code{CDR3L} or \code{CDR3}.}

\item{cell_id_col}{Name of the column with cell IDs (default: "cell_id").}

\item{barcode_format}{Character indicating format of barcode in \code{cell_id}.
Options: \code{"WELL_PLATE"} (e.g., A01P09), \code{"PLATE_WELL"} (e.g., P09A01), or \code{"auto"} to detect automatically. Default: \code{"WELL_PLATE"}.}
}
\value{
A list with:
\itemize{
\item \code{collapsed}: A data frame with collapsed contigs and background tags.
\item \code{real_contigs_input}: Original input filtered to only contigs classified as REAL.
}
}
\description{
Parses cell IDs, identifies and classifies contigs from the same well as real or background.
Contigs with the same clonality ID are collapsed; different clonalities in the same tree are BG1;
unrelated trees are BG2. Also returns filtered original input for contigs tagged as REAL.
}
